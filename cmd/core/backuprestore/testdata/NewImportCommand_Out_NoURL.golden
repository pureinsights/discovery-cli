Usage:
  import [subcommands] [flags]

Examples:
	# Import the entities using profile "cn" and update conflict resolution strategy.
	discovery core import -p cn --file "entities/core.zip" --on-conflict UPDATE

Flags:
  -f, --file string          the file that contains the entities that will be restored
  -h, --help                 help for import
      --on-conflict string   the conflict resolution strategy that will be used (default "FAIL")
  -p, --profile string       configuration profile to use (default "default")

